<p id="notice"><%= notice %></p>

<h1 class="is-size-1" style="color: <%=@category.color%>;"><%=@category.name %></h1>
<h2 class="is-size-3" >Artículos</h2>
<% @category.articles.each do |article| %>
  <h3 class="is-size-4"><%= link_to article.title, article %></h3>
  <div>
    <p class="is-size-6">
      <%= article.body %> - <%= link_to "Eliminar",article,class: "red",method: :delete %>
    </p>
  </div>
<%end %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
